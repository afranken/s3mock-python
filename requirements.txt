# Deprecated: dependency management moved to pyproject.toml (PEP 621) and uv.
# Prefer using:
#   uv venv && uv sync --group dev
# The below is a fallback for legacy environments and may lag behind.

boto3>=1.34
boto3-stubs[s3]>=1.34
pytest>=8.2
testcontainers[core]>=4.6
